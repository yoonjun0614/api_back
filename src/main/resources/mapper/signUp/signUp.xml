<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.api.api.signUp.mapper.SignUpMapper">

    <insert id="insertUser" parameterType="com.api.api.signUp.vo.SignUpVO">
        INSERT INTO project.`user`
            (login_id, name, password, email, phone, reg_dt)
        VALUES(#{loginId}, #{name}, #{password}, #{email}, #{phone}, CURRENT_TIMESTAMP);
    </insert>
</mapper>