<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.api.api.member.mapper.MemberMapper">

    <select id="getMemberList" parameterType="com.api.api.member.vo.MemberVO" resultType="com.api.api.member.vo.MemberVO">
        /* getMemberList */
        SELECT uid, login_id as loginId, name, `password`, email, phone ,date_format(reg_dt, '%Y-%m-%d %H:%i:%s') as regDt, date_format(upt_dt, '%Y-%m-%d %H:%i:%s') as uptDt
        FROM project.`user`
        WHERE 1=1
    </select>

</mapper>